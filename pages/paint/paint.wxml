              <view class='photo' bindtap="chooseImageFun">
                <view class='a' wx:if="{{flag}}">头像</view>
                <image src="{{imageUrl}}"></image>
            </view>
<view class="section">
  <input  style='width:100%;height:100%;font-size:50rpx;'  maxlength="48" />

</view>
    <canvas canvas-id="myCanvas"  style='width:100%;height:1100rpx'  function="{{onReady}}">
  </canvas>
  <view class='submit'>
      <button formType="submit" class="Btn generateBtn" bindtap="Okgenerate"> 保存</button>  
  </view>
